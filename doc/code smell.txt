【✔】TouchControl _currentDock 状态令人不适。可以理解对于 touch 这个空间 位置坐标很便利也容易理解。
假设这个状态原本就存在于这个控件，那么它应该根据 TouchRect Container 的大小和坐标，自适应变化。
一，动画释放结束时，是一个更新时机
二，窗口大小改变时，是一个更新时机
我需要以更新时机为源，去取 TouchRect 和 Container 俩个状态，来计算新的 _currentDock。

 container.Events().SizeChanged
     .Select(windowSize =>
     ...
     .Select(pair => PositionCalculator.CalculateTouchDockRect(pair.WindowSize, pair.TouchDock, pair.TouchSize))
     .Subscribe(SetTouchDockRect);
外部依赖的窗口变化，在 client size 订阅之时，确确实实的会发出新的item，相当于冷 observable
内部依赖的窗口变化，是 winui 控件本身的大小布局。
更关键的是，一定要保证能够确认几个时间点，他们100%能够触发，比如 client 订阅之际，一定会发出 size 没问题。
1. TouchControl.Loaded 只有在这个触发后，才意味着 Touch 的所有属性变化通知布局有所依，订阅完成了。相当于窗口初始化完成。
2. 后台任务的 client size 发出窗口大小便于定位窗口的时机
3. OnTouchShowed 意义不明，目前只是为了刷新或触发 Touch 的透明事件，不然要不会提前处理完，要不永远不会处理。
我需要知道的是他们现在的一个执行顺序，谁先谁后，不论游戏是已经启动还是后启动，需要明确一个确切的固定的先后顺序
